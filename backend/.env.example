# Server Configuration
PORT=5000
NODE_ENV=development

# ─────────────────────────────────────────────────────────
# DATABASE — choose ONE of the two options below
# ─────────────────────────────────────────────────────────

# OPTION A: Cloud / Render / Railway / Supabase / Neon
# Set DATABASE_URL and leave the DB_* vars empty.
# The connection string includes host, port, name, user and
# password all in one, e.g.:
#   postgresql://user:password@host:5432/dbname
DATABASE_URL=

# OPTION B: Local development (used when DATABASE_URL is empty/unset)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=sales_dashboard
DB_USER=postgres
DB_PASSWORD=your_password_here

# CORS
CORS_ORIGIN=http://localhost:5173
